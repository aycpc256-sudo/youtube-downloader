COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# ★ 기존 yt-dlp 완전 제거 후 master 빌드 설치
RUN pip uninstall -y yt-dlp || true

RUN pip install --no-cache-dir --no-deps \
    "https://github.com/yt-dlp/yt-dlp/archive/refs/heads/master.tar.gz"

# ★ 설치 확인 (빌드 로그에 버전 찍힘)
RUN python -c "import yt_dlp; print('YT_DLP_VERSION_CHECK:', yt_dlp.version.__version__)"

COPY server.py .
